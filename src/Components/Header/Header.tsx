import logo from '../../Assets/img/logo.svg'
import './Header.scss'

function Header() {
  return (
    <header className='header'>
      <img src={logo} alt='' />
    </header>
  )
}

export default Header
